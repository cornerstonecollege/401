#include "util.h"

int addSum(int x, int y)
{
  return x + y;
}

int subSum(int x, int y)
{
  return x - y;
}

double divSum(double x, double y)
{
  return x / y;
}

int multiSum(int x, int y)
{
  return x * y;
}
